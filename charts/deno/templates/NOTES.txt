1) Modifier le fichier /etc/hosts: echo "$(minikube ip) deno.minikube" | sudo tee -a /etc/hosts
2) Pour accéder au serveur web aller sur: http://{{ .Values.server.web.service.host }}

Avec helm:

1)  helm repo add miroyld https://miroyld.github.io/deno-webserver/
2)  helm install deno-webserver miroyld/deno-webserver